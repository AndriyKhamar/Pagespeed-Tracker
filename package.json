{
  "name": "pagespeed-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "collect": "node scripts/collect.mjs",
    "validate": "node scripts/validate-config.mjs"
  }
}
